import { PawnComponentBase } from './pawn-component.base';

describe('PawnComponent.Base', () => {
  it('should create an instance', () => {
    expect(new PawnComponentBase()).toBeTruthy();
  });
});
